import { Message } from "../src/message"

describe("testsuite for message", () => {

    test('测试解析', () => {
        let message: Message = new Message();
        message.setID(123)
        message.settopic('/zebus')
        message.setattempts(10);
        message.setbody("{\"test\":\"test\"")
        let buffer = message.getBuffer();
        console.log('buffer', buffer)
    })
})