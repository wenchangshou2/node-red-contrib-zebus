import { Buffer } from './buffer';
export declare class LRUBuffer<E> implements Buffer<E> {
    private readonly buffer;
    private writePtr;
    private wrapped;
    constructor(len: number);
    len(): number;
    cap(): number;
    read(es: E[]): number;
    write(es: E[]): number;
    forEach(fn: (e: E) => any): number;
    clear(): void;
}
//# sourceMappingURL=lrubuffer.d.ts.map